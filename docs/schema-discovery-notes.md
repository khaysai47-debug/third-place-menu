# Schema Discovery Notes — filled ____-__-__ (STATUS: NOT STARTED)

Blank worksheet for Phase 2B. How to fill it: `docs/schema-discovery-guide.md`.
Write `NOT FOUND` rather than guessing; partial is fine — blanks just stay
blocked. Copy real values verbatim (casing included).

**⚠️ NO SECRETS IN THIS FILE.** This file is committed to git. Never paste an
anon key, service_role key, password, or full connection string here — only
note *where* the credential lives (e.g. "n8n credential: Supabase account
'third-place'") and *which kind* it is.

## Supabase project / connection
- Project URL (the `https://<ref>.supabase.co` part only — safe to record):
- Key type n8n uses (anon / service_role / postgres password — do NOT copy the key):
- Where the credential lives in n8n (credential name):
- Any table exposed via `supabase.co/rest/v1/...` in HTTP Request nodes:

## Orders table
- Table name:
- Primary key column + example value:
- Human order number column (TP-…):
- All other columns (name → example value):

## Items (table or JSON shape)
- Stored as: [ ] JSON column on orders  [ ] separate table  [ ] other:
- Column/field names for one line (name, quantity, unit price, item id):
- One real example line, copied verbatim:

## Expenses table
- Table name:
- Columns (name → example value):
- EXP-… id generated by:
- review_status default:

## Payment proof storage
- Written by workflow:
- Stored as (URL column / storage bucket / attachment):
- Columns (flag, url, status, received_at):

## Status values
- Column name:
- Exact values seen (copy verbatim, with casing):
- done vs completed:

## Payment values
- Payment status column + exact values (casing!):
- Payment method column + exact values:
- paid_at column:

## Delivery values
- Columns for customer name / phone / address / fee / subtotal:

## Cancellation values
- Reason column:
- Cancelled-at column:
- What the cancel workflow writes, verbatim:

## Timestamps & money
- One real timestamp value, verbatim:
- One real money value, verbatim (number or string?):

## Unknown / risky fields
- Anything you saw but didn't understand — paste it here rather than skip it:

---

When every section above has an answer or `NOT FOUND`, change STATUS in the
title line to FILLED and Phase 2C can start
(`docs/backend-separation-runbook.md`).
